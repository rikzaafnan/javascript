const panggiljoin = () => {
    var kota = ['jakarta', 'medan', 'malang', 'palembang', 'bandung']
    console.log(kota)

    var result = kota.join(",")
    return result
}

console.log(panggiljoin())